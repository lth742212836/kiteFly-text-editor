# Changelog

## 2026-06-04

### 新增

- **语法高亮开关**：状态栏新增「高亮 开 / 高亮 关」按钮，可手动切换当前文件的语法高亮。大文件（>2MB）默认关闭高亮以保证性能，小文件默认开启。
- **主题切换系统**：新增黑夜、白天、护眼三套界面主题。工具栏新增主题切换按钮，循环切换三套主题。主题偏好自动持久化到 localStorage。
  - 黑夜：VS Code 暗色风格，深色背景 + 浅色文字
  - 白天：亮色风格，白色背景 + 深色文字
  - 护眼：浅绿色背景 + 深绿色文字，适合长时间阅读
- **侧边栏折叠功能**：「最近打开」侧边栏新增折叠按钮，点击可将整个侧边栏收起，编辑器自动填充空间。折叠后保留 32px 窄条（含竖排"最近打开"文字和展开图标），点击即可重新展开。
- **Monaco 编辑器护眼主题**：通过 `monaco.editor.defineTheme` 自定义注册 `eye-care` 主题，编辑器背景与全局护眼配色保持一致。

### 优化

- **超大文件加载策略**：去掉 50MB 文件大小的确认弹窗，改为直接加载文件但自动禁用语法高亮，仅显示纯文本内容，提升大文件打开体验。
- **拖拽文件刷新侧边栏**：通过拖拽打开的文件会立即显示在侧边栏「最近打开」列表中。
- **关闭 Unicode 歧义字符提示**：禁用 Monaco Editor 的 `unicodeHighlight` 配置，不再显示「ambiguous unicode characters」警告。
- **状态栏语言模式显示修复**：大文件打开后状态栏正确显示文件类型（如 SQL、JavaScript 等），不再一律显示 `plaintext`。`tab.language` 始终保存真实语言模式，语法高亮仅通过编辑器模型切换控制。
- **删除最近文件不再弹窗确认**：点击侧边栏最近文件记录的删除按钮，直接删除不再弹出确认对话框。
- **全局样式主题化**：工具栏 tooltip、状态栏编码选择器、滚动条等组件颜色全部改用 CSS 变量，随主题切换自动变化。

### 修复

- **删除最近文件后仍残留**：修复侧边栏删除最近文件后文件仍显示的问题，通过 `stopPropagation` 阻止事件冒泡导致重新添加。
- **高亮开关点击无响应**：新增 `highlightVersion` 计数器确保跨组件响应式更新可靠触发，切换时同步更新编辑器语言模式和选项。
- **关闭所有标签页后编辑器仍显示内容**：当所有标签页关闭后，清空 Monaco Editor 模型内容，不再残留之前文件的内容。
- **切换标签页后主题颜色丢失**：修复切换标签页或点击最近打开文件时编辑器主题回退到默认值的问题。将 `theme` 从静态 `baseEditorOptions` 中移除，改为在 `updateEditorContent` 中每次动态设置当前主题。

---

## 2026-06-03

### 优化

- **编码选择器改为白色背景**：状态栏编码下拉框改为白底深色文字，在蓝色状态栏上更加醒目。

- **侧边栏"最近打开"文件切换速度优化**：点击已打开的文件时，跳过 IPC 文件读取，直接切换标签页。新增 `activateTabByPath` 方法，纯内存操作实现近乎即时的标签页切换。

### 修复

- **工具栏面板互斥切换**：修复查找/替换面板点击工具栏按钮无法关闭的问题，改为 toggle 逻辑。

### 改进

- **工具栏按功能分组**：文件/保存/编辑/搜索/视图五个功能组，图标统一为 Feather SVG 风格。
- **工具栏悬浮提示**：按钮 hover 0.4 秒后显示功能名称和快捷键提示。
- **平台适配快捷键显示**：macOS 平台 Ctrl 自动显示为 ⌘，其他平台保持 Ctrl。
- **编码选择器移至状态栏**：从编辑器上方移至底部状态栏，交互式下拉选择。
